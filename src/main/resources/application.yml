spring:
  application:
    name: echoVoice-user-service
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      org:
        hibernate:
          envers:
            audit_table_suffix: _audit
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://ec2-54-159-175-38.compute-1.amazonaws.com:5432/d87n6mi2uul7ot
    username: mljoivfzroyjcn
    password: ea03e71eb1d81ebd06a55e1b30d7d4a4e4ef1cbc734cbfa9119e0e1fde0741c5

  liquibase:
    change-log: classpath:/liquibase/db.changelog.xml
  mvc:
    servlet:
      path: "/${spring.application.name}"

server:
  port: 8080
  #servlet:
  #  context-path: "/${spring.application.name}"
